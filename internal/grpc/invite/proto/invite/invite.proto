syntax = "proto3";

package invite;

option go_package = "github.com/werbot/werbot/internal/grpc/invite/proto/invite";

service InviteHandlers {}

enum Action {
  token_action_unspecified = 0;
  register = 1;
  team = 2;
  reset = 4;
  delete = 5;
}
